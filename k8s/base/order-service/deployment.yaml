apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: ecom-prod
  labels:
    app: order-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
        - name: order-service
          image: <account_id>.dkr.ecr.us-east-1.amazonaws.com/order-service:GIT_SHA
          ports:
            - containerPort: 5004
          envFrom:
            - configMapRef:
                name: order-service-config
            - secretRef:
                name: order-service-secrets
          readinessProbe:
            httpGet: { path: /health, port: 5004 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 5004 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits: { cpu: "500m", memory: "256Mi" }
