# Placeholder — in real deployments this Secret is synced from AWS Secrets Manager
# via the External Secrets Operator (see docs/Engineering-Handbook.md, Section 8.2).
# Do NOT commit real credentials here.
apiVersion: v1
kind: Secret
metadata:
  name: product-service-secrets
  namespace: ecom-dev
type: Opaque
stringData:
  DATABASE_URL: "postgresql://REPLACE_ME@rds-endpoint:5432/ecom_product"
