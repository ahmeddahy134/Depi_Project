apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: ecom-prod
spec:
  selector:
    app: notification-service
  ports:
    - port: 80
      targetPort: 5005
  type: ClusterIP
